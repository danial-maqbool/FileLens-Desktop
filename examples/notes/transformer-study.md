# Transformer study notes

Attention compares a query with keys and uses the scores to combine values.
A transformer uses attention blocks and feed-forward layers.
This note covers embeddings, token positions, attention masks, and evaluation.
Prepare the machine learning quiz with short numerical examples.

# Release process

The release branch runs platform tests, native integration tests, a dependency audit, and a browser recording. Promote it to main only after all required jobs pass. Do not force-push unrelated history. Every installed application remains self-contained and works without GitHub access.
